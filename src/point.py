from tkinter import Tk, BOTH, Canvas

class Point():
    def __init__(self, x, y):
        self.x = x # x = 0 is the leftmost side of the window
        self.y = y # y = 0 is the topmost side of the window
